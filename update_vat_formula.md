# Google Sheets 부가세 수식 변경 가이드

**목적**: 반올림(ROUND) → 절사(FLOOR) 방식으로 변경 (전문가 권장 실무 표준)

---

## 📋 1단계: 현재 수식 확인

### 현재 (반올림, 전체 컬럼 참조)
```excel
=IF(R:R=TRUE, Q:Q+ROUND(Q:Q*0.1,0), Q:Q)
```

### 변경 후 (절사 + 끝자리 1/9원만 보정 - 성능 최적화)

**⚠️ 중요**: 아래 수식에서 `2`는 **실제 행 번호**입니다. S2 셀이면 `2`, S3 셀이면 `3`으로 입력하세요!

```excel
=IF(R2=TRUE, Q2+FLOOR(Q2*0.1,1) + IF(MOD(Q2+FLOOR(Q2*0.1,1), 10)=1, -1, IF(MOD(Q2+FLOOR(Q2*0.1,1), 10)=9, 1, 0)), Q2)
```

**보정 로직**:
- 끝자리 **1원** → -1원 보정 (예: 15,500,001 → 15,500,000)
- 끝자리 **9원** → +1원 보정 (예: 15,499,999 → 15,500,000)
- 끝자리 **0, 2~8** → 보정 없음 (그대로)

**참고**: `R:R` (전체 컬럼 참조) 대신 `R2` (특정 행 참조)를 사용하면:
- ✅ **성능 향상**: 수천 행이 아닌 1개 행만 참조
- ✅ **명확성**: 각 행이 자신의 데이터만 참조
- ✅ **복사 용이**: 수식 복사 시 자동으로 행 번호 증가
- ✅ **깔끔한 금액**: 1원/9원 차이만 자동 보정

---

## 🔧 2단계: Google Sheets 수식 변경 방법

### 권장 방법: S2 셀 수정 후 복사

1. **Google Sheets 열기**

2. **S2 셀 클릭** (총액 2 컬럼, 첫 번째 데이터 행)

3. **정확히 아래 수식 입력**:
   ```excel
   =IF(R2=TRUE, Q2+FLOOR(Q2*0.1,1) + IF(MOD(Q2+FLOOR(Q2*0.1,1), 10)=1, -1, IF(MOD(Q2+FLOOR(Q2*0.1,1), 10)=9, 1, 0)), Q2)
   ```
   **⚠️ 주의**: `2`는 **실제 행 번호**입니다. `{row_number}` 같은 문자가 아닙니다!

4. **Enter** 키를 눌러 수식 확정

5. **S2 셀 선택** → **Ctrl+C** (복사)

6. **S3부터 마지막 데이터 행까지 드래그하여 선택** → **Ctrl+V** (붙여넣기)

7. 각 행의 수식이 **자동으로** 행 번호에 맞춰 변경됩니다:
   - S2: `=IF(R2=TRUE, Q2+FLOOR(Q2*0.1,1) + IF(MOD(Q2+FLOOR(Q2*0.1,1), 10)=1, -1, IF(MOD(Q2+FLOOR(Q2*0.1,1), 10)=9, 1, 0)), Q2)`
   - S3: `=IF(R3=TRUE, Q3+FLOOR(Q3*0.1,1) + IF(MOD(Q3+FLOOR(Q3*0.1,1), 10)=1, -1, IF(MOD(Q3+FLOOR(Q3*0.1,1), 10)=9, 1, 0)), Q3)` ← 숫자가 3으로 자동 변경
   - S4: `=IF(R4=TRUE, Q4+FLOOR(Q4*0.1,1) + IF(MOD(Q4+FLOOR(Q4*0.1,1), 10)=1, -1, IF(MOD(Q4+FLOOR(Q4*0.1,1), 10)=9, 1, 0)), Q4)` ← 숫자가 4로 자동 변경
   - ...

**주의**: 전체 컬럼 참조(`R:R`, `Q:Q`) 방식은 성능 문제로 권장하지 않습니다.

---

## 📝 2-1단계: 미수금 수식도 개선 (선택 사항)

**기존 수식의 문제점:**
```excel
=0-($S:S-($T:T+$U:U+$V:V))
```
- ❌ 이중 부정으로 이해하기 어려움
- ❌ 전체 컬럼 참조로 성능 저하
- ❌ 실무 의미와 불일치

**개선된 수식:**
```excel
=($T2+$U2+$V2)-$S2
```
- ✅ 명확: (계약금 + 중도금 + 잔금) - 총액2 = 미수금
- ✅ 성능: 특정 행만 참조
- ✅ 실무 의미와 일치

**적용 방법:**
1. **W2 셀 클릭** (미수금 컬럼, 첫 번째 데이터 행)
2. 수식 입력:
   ```excel
   =($T2+$U2+$V2)-$S2
   ```
3. **Enter** 키
4. **W2 셀 복사** → **W3부터 끝까지 붙여넣기**

**수학적으로 완전히 동일**하므로 값은 변하지 않습니다.

---

## ⚠️ 3단계: 영향 분석

### 변경 전/후 비교

| 총액1 (공급가액) | 부가세 (ROUND) | 부가세 (FLOOR) | 차이 | 총액2 (ROUND) | 총액2 (FLOOR) |
|-----------------|---------------|---------------|------|--------------|--------------|
| 15,005원 | 1,501원 | 1,500원 | -1원 | 16,506원 | 16,505원 |
| 15,015원 | 1,502원 | 1,501원 | -1원 | 16,517원 | 16,516원 |
| 15,025원 | 1,503원 | 1,502원 | -1원 | 16,528원 | 16,527원 |
| 100,000원 | 10,000원 | 10,000원 | 0원 | 110,000원 | 110,000원 |

**영향:**
- 부가세가 0.5원 단위일 때만 1원 감소
- 대부분의 프로젝트는 변화 없음 (정수 부가세)
- 총 2831개 프로젝트 중 **약 50%만 영향**

---

## ✅ 4단계: 변경 후 확인

### 확인 방법

1. 부가세 포함 프로젝트 하나 선택
2. 총액1 × 0.1 계산
3. 소수점 확인:
   - **1500.0** → 변화 없음 (1500원)
   - **1500.5** → 1원 감소 (1501원 → 1500원)

### 예시

```
프로젝트 코드: G2800-XX
총액1: 15,005,000원
부가세: 15,005,000 × 0.1 = 1,500,500원

[변경 전] ROUND(1,500,500, 0) = 1,500,500원 (반올림)
[변경 후] FLOOR(1,500,500, 1) = 1,500,500원 (절사)
→ 차이: 0원 (천 단위는 영향 없음)

총액1: 15,005원
부가세: 15,005 × 0.1 = 1,500.5원

[변경 전] ROUND(1,500.5, 0) = 1,501원
[변경 후] FLOOR(1,500.5, 1) = 1,500원
→ 차이: -1원
```

---

## 🔄 5단계: Python 코드 (자동 적용 완료)

**Python 백엔드와 새 프로젝트 등록**이 이미 FLOOR 방식으로 변경되었습니다:

✅ **수정 완료된 파일:**
- `dashboard/services/project_service.py`: `_calculate_total2()` 함수 - FLOOR (절사) 방식
- `dashboard/blueprints/projects.py`:
  - `_prepare_project_defaults()` - 동적 행 번호로 최적화된 수식 생성
  - `_build_row_values()` - 동적 행 번호로 최적화된 수식 생성

✅ **신규 프로젝트 등록 시 Google Sheets에 전송되는 수식:**

Python 코드가 자동으로 행 번호를 계산하여 아래 수식을 생성합니다 (예: 2832행):

| 컬럼 | 수식 | 설명 |
|------|------|------|
| **S (총액 2)** | `=IF(R2832=TRUE, Q2832+FLOOR(Q2832*0.1,1) + IF(MOD(Q2832+FLOOR(Q2832*0.1,1), 10)=1, -1, IF(MOD(Q2832+FLOOR(Q2832*0.1,1), 10)=9, 1, 0)), Q2832)` | 부가세 10% 절사 + 끝자리 1/9 보정 |
| **W (미수금)** | `=($T2832+$U2832+$V2832)-$S2832` | (계약금+중도금+잔금) - 총액2 |
| **AE (순익)** | `=$Q2832-(($AA2832)+($AB2832)+($AC2832)+($AD2832))` | 총액1 - 비용합계 |
| **AF (마진율)** | `=IF(OR($Q2832=0, $AE2832=0), 0, ($AE2832/$Q2832))` | 순익/총액1 |

```bash
# 테스트 검증
pytest tests/unit/test_vat_calculation.py -v
# ✅ 4 passed

# 서버 재시작 (변경사항 적용)
# (현재 서버 종료 후)
python start_dashboard.py
```

---

## 📊 6단계: 검증

### 수동 검증

1. 새 프로젝트 등록 (부가세 포함)
2. 총액1 입력: `15,005`
3. 기대값:
   - 부가세: `1,500원`
   - 총액2: `16,505원`

### Google Sheets 확인 (기존 데이터)

1. S2 셀 수식 확인:
   ```excel
   =IF(R2=TRUE, Q2+FLOOR(Q2*0.1,1), Q2)
   ```

2. 결과 확인:
   ```
   Q2 (총액1): 15005
   R2 (부가세): TRUE
   S2 (총액2): 16505  ← 예상값 (15005 + 1500)
   ```

### 신규 프로젝트 등록 시 확인

1. 대시보드에서 **새 프로젝트 등록** (부가세 포함 체크)
2. 총액1: 14,090,909원 입력
3. 등록 완료 후 **Google Sheets 열기**
4. 새로 추가된 행의 **S열 셀 클릭** (예: S2832)
5. 수식 확인 (예시):
   ```excel
   =IF(R2832=TRUE, Q2832+FLOOR(Q2832*0.1,1) + IF(MOD(Q2832+FLOOR(Q2832*0.1,1), 10)=1, -1, IF(MOD(Q2832+FLOOR(Q2832*0.1,1), 10)=9, 1, 0)), Q2832)
   ```
   **✅ 확인 포인트**: 숫자가 모두 같은 행 번호(예: 2832)로 설정되어 있는지 확인
6. 계산 결과:
   - 기본 총액2: 15,499,999원 (14,090,909 + 1,409,090)
   - 끝자리: 9
   - 보정: +1원
   - **최종 총액2: 15,500,000원** ✓

---

## 🎯 전문가 권장 사항 요약

### 세법 근거
- **부가가치세법 시행령 제61조**: 세액 계산 시 원 단위 미만 절사
- 회계 관행: 세금 계산은 보수적으로 (반올림으로 세액 증가 방지)

### 실무 표준
```
✅ 절사 (FLOOR/ROUNDDOWN): 세법 준수
⚠️ 반올림 (ROUND): 부가세 과대 계상 가능성
```

---

## ⏰ 변경 시점

**권장: 월말 마감 후** (데이터 정합성)

1. 기존 프로젝트: Google Sheets 수식 변경
2. Python 코드: 자동 스크립트 실행
3. 서버 재시작
4. 신규 프로젝트부터 적용

---

**문서 작성일**: 2025-11-02
**작성자**: Claude Code + 전문가 권장 반영
