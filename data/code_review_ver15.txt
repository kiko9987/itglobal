 잘하고 있는 점 (낮은 위험도)

  - dashboard/templates/modern_project_list.html:5-10에서 Vite CSS 번들을 기본으로 로드하고, dashboard/src/css/
  main.css:5-14가 design-system 토큰 → 공통 컴포넌트 → 페이지 전용 스타일(project-list-page.css)을 순차적으로 import하면
  서 모듈화된 구조가 실제 화면에 적용됩니다.
  - 인라인 스타일을 대부분 걷어내고(modern_project_list.html:18-40, :48-76 등) project-header, cache-status-container 같
  은 클래스로 치환하여 src/css/components/project-list-page.css에 응집시킨 점이 깔끔합니다.
  - ProjectTable 컴포넌트는 manifest에 CSS를 함께 기록(dashboard/static/dist/.vite/manifest.json의 css/ProjectTable.*)하
  여 JS 모듈 로딩 시 자동으로 스타일이 연결되도록 구성되었습니다.

  아쉬운 점 (중간 위험도)

  - src/css/design-system/tokens.css와 src/css/main.css가 여전히 body, shadow 등 동일한 속성을 중복 선언합니다. 토큰만
  수정했을 때 이중 정의 때문에 어느 쪽이 우선될지 예측하기 어렵습니다.
  - project-list-page.css에 열 너비(예: 21%, 17% 등), modal 레이아웃 같은 레거시 수치를 그대로 가져온 부분이 많습니다.
  향후 테이블 구조가 바뀌면 또 다시 손수 맞춰야 할 가능성이 큽니다.
  - fallback 체계가 여전히 /static/legacy/css/project-list.css/project-list.js에 강하게 의존합니다(utils/
  frontend_helpers.py:172-285). 모던 자산이 비었을 때 문제를 막아주지만, 레거시 자산 정리가 끝날 때까지는 매번 이 복잡한
  맵핑을 유지해야 합니다.

  고쳐야 할 점 (높은 위험도 → 낮은 위험도 순)

  1. 높음 – design-system 중복 해소
      - 토큰 파일을 진리로 삼고 main.css에서는 더 이상 색상·폰트 등 기본 토큰을 재선언하지 않도록 정리하세요. 그렇지 않
  으면 색상/폰트 변경 시 두 파일을 모두 수정해야 합니다.
  2. 중간 – 페이지 전용 스타일 정리
      - project-list-page.css에 박혀 있는 고정 폭/px 값을 토큰 기반 spacing으로 대체하고, 불필요한 width 지정은
  DataTables 옵션이나 CSS Grid로 넘겨 유지보수를 수월하게 만드세요.
  3. 낮음 – 폴백 경량화
      - 레거시 CSS/JS가 실제 사용되지 않는 화면에서는 mapping 테이블에서 제거해 나가고, 최종적으로는 폴백 자체를 대폭 단
  순화하거나 없애는 일정을 세우는 게 좋습니다.

  추천 작업 순서

  1. design-system 토큰을 기준으로 main.css·component CSS의 중복 정의를 제거합니다.
  2. 테이블/모달 등 페이지 전용 스타일에서 하드코딩된 수치를 토큰·유틸 클래스로 전환해 유지보수 부담을 낮춥니다.
  3. 레거시 폴백 매핑을 실제 필요 목록으로 줄이고, 최종적으로 Vite 자산만으로 서비스 가능한지 검증하세요.