우선순위

> - 렌더링 경로부터 정리 필요. blueprints/projects.py:51가 여전히 project_list.html을 반환하면서 자산 매니저(utils/
  frontend_helpers.py)가 준비한 manifest 경로를 전혀 쓰지 못합니다. 모던 템플릿(templates/modern_base.html, templates/
  modern_project_list.html)을 기본 진입점으로 전환하고, 레거시 화면은 별도 플래그로 분리해야 이후 작업이 단순해집니다.
  - 자산 관리 계층을 먼저 굳히는 게 우선입니다. FrontendAssetManager._get_fallback_url이 항상 None을 돌려 지금 구조에서
  는 manifest가 없으면 사이트가 그대로 깨집니다. npm run build 실패나 개발 모드에서도 견딜 수 있도록 정적 경로 fallback
  을 구현하고, 앱 기동 시 manifest 유무를 검사해 로그를 남기는 초기화 절차를 추가하세요.
  - 프런트 빌드는 아직 완전히 모듈화되지 않았습니다. src/js/pages/project-list.js 한 파일에 800줄 넘는 초기화/비동기/DOM
  로직이 모두 들어가 있어 유지보수가 어렵고 번들도 무겁습니다. 데이터 로딩, UI 컴포넌트, 상태 저장을 각각 별도 클래스로
  나눠 app.init()이 orchestration만 담당하게 리팩터링하는 것이 좋습니다.
  - 서버와 클라이언트 간 데이터 계약도 명확히 해야 합니다. blueprints/projects.py:63에서 템플릿 렌더용으로 대량의 프로젝
  트 데이터를 통째로 넘기지만, JS 측은 /api를 통해 다시 로딩하도록 설계되어 중복이 발생합니다. 초기 페이지에서는 최소 메
  타 정보만 제공하고 상세 데이터는 API로만 받도록 계약을 단일화하세요.
  - 캐시·동시성 관리 부분은 백엔드에서 철저히 다루고 있지만, 프런트 측 Field Lock/Cache 모듈이 페이지 init 시점마다
  setInterval을 돌려 주기적으로 상태를 꺼내오게 되어 있습니다(src/js/pages/project-list.js:414 인근). 캐시 갱신·락 상태
  는 서버 푸시나 명시적 user action에 연동하도록 바꿔야 네트워크 낭비와 race condition을 줄일 수 있습니다.
  - 빌드/배포 단계도 일원화해야 합니다. package.json에서 Vite 빌드를 정의해 놓고도 실제 Flask 템플릿은 CDN에 의존하는 구
  조입니다. CI나 배포 스크립트에서 npm run build → manifest 검증 → Flask 서비스 재기동 순서를 자동화하고, 실패 시 레거시
  CSS 로드로 롤백되는 안전장치를 마련하세요.
  - 스키마·서비스 계층을 재검토할 시점입니다. services/project_service.py에서 구글시트/캐시/동시성 처리를 모두 직접 다루
  는데, 이 로직이 블루프린트까지 파고들어 있습니다. 데이터 접근을 API 엔드포인트로만 노출하고, 템플릿 렌더링용 함수는 최
  소한의 어댑터만 제공하도록 레이어를 분리하면 이후 SPA 전환이나 모바일 클라이언트 확장에 유리합니다.


> Current Issues

  - static/css/project-list.css:97 여전히 레거시 헤더 레이아웃(고정 패딩·216px 로고·수동 transform) 규칙이 살아 있어 전
  체 헤더 정렬과 높이가 무너집니다. 같은 파일의 모바일 대응 블록(static/css/project-list.css:1937, static/css/project-
  list.css:1999, static/css/project-list.css:2036)도 더 이상 존재하지 않는 .col-md-8/.col-md-4 그리드를 전제로 버튼 폭·
  정렬을 강제로 바꾸며_viewport_별로 레이아웃이 망가집니다.
  - src/css/components/base.css:35와 src/css/components/base.css:196 등에서 rgba(var(--primary-color), 0.1)처럼 HEX 변수
  를 그대로 rgba()에 넣어 버려 브라우저가 색상을 파싱하지 못합니다. 이 때문에 헤더 버튼·로딩 오버레이 같은 핵심 색상 스
  타일이 전부 빠지고 있습니다.
  - src/css/components/base.css:10에는 var(--zindex-sticky)를 기대하지만 :root 어딜 찾아도 정의가 없습니다. 동일하게
  var(--loading-overlay-bg) 등도 정의가 없어 z-index·배경색이 전부 기본값으로 처리되고 있습니다.
  - src/css/main.css:48 이후 구간은 $font-family-base, $gray-800 같은 SCSS 변수를 그대로 남긴 채 Vite에 .css로 전달되고
  있습니다. 컴파일이 되지 않으니 본문/헤더 색상·폰트 설정이 죄다 무시되고, 메인 번들(static/dist/css/main.C6I45UMF.css)
  에도 같은 문제가 그대로 묻어 나옵니다.
  - 서버는 여전히 templates/base.html:1과 templates/project_list.html:1에 의존합니다. modern_base.html/
  modern_project_list.html은 만들어 놨지만 blueprints/projects.py:54가 레거시 템플릿만 렌더링하면서 신규 빌드 자산
  (get_css_bundle, get_js_bundle) 호출 경로가 막혀 있습니다. 결국 새 자산 관리 코드는 작동하지 않고, 오래된 CSS가 계속
  우선 적용되는 구조입니다.

  Improvement Plan

  - 새 스타일을 쓸 페이지에서는 static/css/project-list.css 같은 레거시 규칙을 로딩하지 않거나, 최소한 헤더 관련 블록을
  네임스페이스별로 분리하세요. 필요하면 레거시 템플릿에만 별도 레이아웃 클래스를 두고, 현대화 템플릿은 깨끗한 새 CSS만
  물리도록 분기하는 편이 안전합니다.
  - 색상 변수는 --primary-color-hex와 --primary-color-rgb를 나눠 선언하거나, SCSS 단계에서 rgba($primary-color, 0.1)을
  실제 RGBA 값으로 변환해 출력하도록 하세요. 동시에 --zindex-sticky, --loading-overlay-bg 등 CSS 변수도 :root에 추가해
  레이어/배경 스타일이 복구되게 해야 합니다.
  - src/css/main.css를 main.scss로 이름 변경하고 @use 'variables.scss'; 등으로 SCSS를 정식으로 컴파일하세요. Vite 엔트
  리(vite.config.js:17)도 .scss로 갱신하고, 필요한 경우 전역에서 공통 변수를 노출할 수 있도록 :root 출력 블록을 추가하십
  시오.
  - Flask 측 템플릿 라우팅을 정리해 modern_base.html 경로를 기본으로 삼되, 필요하면 플래그로 레거시 템플릿을 fallback으
  로 남기는 구조를 도입하세요. 이 과정에서 get_css_bundle/get_js_bundle이 반드시 호출되도록 하고, 실패 시 fallback URL을
  반환하도록 FrontendAssetManager._get_fallback_url도 보완해야 합니다.
  - 프런트 빌드와 서버 간 배포 파이프라인을 문서화하고, npm run build 결과물(static/dist)이 항상 최신과 일치하는지 CI나
  배포 스크립트에서 검증하세요. 지금처럼 수동 복사로 남겨두면 재현 불가 이슈가 반복됩니다.

  다음 행동 제안

  1. 헤더/프로젝트 목록 템플릿에서 레거시 CSS를 제거하거나 네임스페이스화해 현재 뷰가 어떤 스타일을 쓰는지 명확히 구분하
  세요.
  2. main.scss 빌드 환경을 정비하고 변수 정의를 보강한 뒤 Vite 빌드를 재실행하여 실제 결과 CSS를 검증하세요.
  3. Flask 템플릿 라우팅을 modern_* 파일로 전환하는 브랜치를 만들어, 새 자산 로딩 경로와 서비스 전반이 정상 동작하는지
  통합 테스트를 돌려보세요.