================================================================================
아코디언 헤더 편집 기능 구현 계획 (업데이트)
================================================================================

작성일: 2025-10-06
추가 요구사항 반영

================================================================================
1. 업데이트된 요구사항
================================================================================

## 1.1 담당자 드롭다운
❌ **하드코딩된 16명 목록 사용 안 함**
✅ **Google Sheets에서 실제 사용 중인 담당자만 표시**
✅ **퇴사자 자동 제외**
✅ **신규 담당자 자동 추가**

```javascript
// Before (하드코딩)
const managerOptions = [
  '박용구', '박정우', '강성환', '박민우', '이근혁', '김호중',
  '아이티', '김단이', '권태훈', '주영민', '심장원', '빈승정',
  '박민재', '조성헌', '황해승', '강민석'
];

// After (동적 생성)
const activeManagers = this.getActiveManagersFromData();
// → ['박정우', '강성환', '박용구', '박민우', '권태훈', '주영민', '빈승정', ...]
```

## 1.2 프로젝트 코드 실시간 업데이트
❌ **저장 후에만 변경**
✅ **사업자/담당자 드롭다운 변경 즉시 헤더 텍스트 업데이트**
✅ **편집 완료 후 DataTable 행의 프로젝트 코드도 업데이트**

```
사용자: 담당자를 "강성환" → "박정우"로 변경

즉시 반영:
[배지] G2890-SH  →  [배지] G2890-JW  (미리보기 아님, 실제 변경)

저장 후:
- 백엔드에 저장
- DataTable 행 ID: row-G2890-SH → row-G2890-JW
- DataTable 셀 텍스트: G2890-SH → G2890-JW
```

================================================================================
2. 담당자 목록 동적 생성
================================================================================

## 2.1 ManagerBadge 활용

```javascript
/**
 * 실제 사용 중인 담당자 목록 가져오기
 * - 퇴사자 제외
 * - 실제 프로젝트에서 사용 중인 담당자만
 * - 알파벳순 정렬
 */
getActiveManagersFromData() {
  // 1. ManagerBadge에서 활성 담당자 기본 목록 가져오기
  const baseManagers = this.unifiedBadgeSystem.managerBadge.getActiveManagers();

  // 2. 현재 DataTable에서 실제 사용 중인 담당자 추출
  const dataTableManagers = new Set();

  if (this.dataTable) {
    this.dataTable.rows().every(function() {
      const rowData = this.data();
      const manager = rowData['담당자'];
      if (manager) {
        dataTableManagers.add(manager.trim());
      }
    });
  }

  // 3. 기본 목록 + 실제 사용 중인 담당자 합치기 (퇴사자는 이미 제외됨)
  const resignedTeam = this.unifiedBadgeSystem.managerBadge.resignedTeam;
  const allManagers = new Set([...baseManagers, ...dataTableManagers]);

  // 4. 퇴사자 제외
  const activeManagers = Array.from(allManagers).filter(m => !resignedTeam.includes(m));

  // 5. 가나다순 정렬
  return activeManagers.sort((a, b) => a.localeCompare(b, 'ko-KR'));
}
```

## 2.2 드롭다운 HTML 생성

```javascript
/**
 * 담당자 드롭다운 생성
 */
generateManagerDropdown(currentManager) {
  const managers = this.getActiveManagersFromData();

  return `
    <select class="header-edit-select" data-field="담당자" data-original-value="${currentManager || ''}">
      <option value="">담당자 선택</option>
      ${managers.map(m => `
        <option value="${m}" ${m === currentManager ? 'selected' : ''}>${m}</option>
      `).join('')}
    </select>
  `;
}
```

================================================================================
3. 프로젝트 코드 실시간 업데이트
================================================================================

## 3.1 클라이언트 사이드 프로젝트 코드 생성

```javascript
/**
 * 프로젝트 코드 생성 (클라이언트 사이드)
 * - 백엔드 project_code_generator.py와 동일한 로직
 */
generateProjectCode(rowNumber, company, manager) {
  // 1. 사업자 → Prefix
  const companyPrefix = {
    '플렌트': 'P',
    '글로벌': 'G',
    '글로벌그룹': 'R'
  };
  const prefix = companyPrefix[company] || '';

  // 2. 행번호 4자리
  const numberPart = String(rowNumber).padStart(4, '0');

  // 3. 담당자 → Suffix
  const managerSuffix = {
    '박용구': '-YG', '박정우': '-JW', '강성환': '-SH', '박민우': '-MW',
    '이근혁': '-GH', '김호중': '-HJ', '아이티': '-IT', '김단이': '-DN',
    '권태훈': '-TH', '주영민': '-YM', '심장원': '-SJW', '빈승정': '-SJ',
    '박민재': '-MJ', '조성헌': '-JSH', '황해승': '-HS', '강민석': '-MS'
  };
  const suffix = managerSuffix[manager] || '';

  // 4. 조합
  return `${prefix}${numberPart}${suffix}`;
}

/**
 * 프로젝트 행번호 가져오기
 */
getProjectRowNumber(projectCode) {
  if (!this.dataTable) return 0;

  const row = this.dataTable.row(`#row-${projectCode}`);
  if (row && row.index() !== undefined) {
    // DataTable index는 0-based, Google Sheets는 2부터 시작 (1행은 헤더)
    return row.index() + 2;
  }

  return 0;
}
```

## 3.2 사업자 드롭다운 추가 (기본정보 카드에서 이동)

현재 "사업자" 필드는 기본정보 카드에 있지만, 프로젝트 코드 생성에 필요하므로 헤더로 이동하거나 동기화 필요.

**옵션 1: 헤더에 사업자 드롭다운 추가**
```html
<div class="project-company-info editable">
  <i class="fas fa-building me-2"></i>
  <select class="header-edit-select" data-field="사업자">
    <option value="플렌트">플렌트</option>
    <option value="글로벌">글로벌</option>
    <option value="글로벌그룹">글로벌그룹</option>
  </select>
</div>
```

**옵션 2: 기본정보 카드의 사업자 변경 감지 (권장)**
```javascript
// 기본정보 카드의 사업자 드롭다운 변경 시 헤더 프로젝트 코드 업데이트
const basicCard = document.getElementById(`card-basic-${projectCode}`);
const companySelect = basicCard.querySelector('[data-field="사업자"]');

companySelect.addEventListener('change', (e) => {
  this.updateHeaderProjectCode(projectCode);
});
```

## 3.3 헤더 프로젝트 코드 실시간 업데이트

```javascript
/**
 * 헤더 프로젝트 코드 실시간 업데이트
 * - 사업자/담당자 변경 시 호출
 */
updateHeaderProjectCode(oldProjectCode) {
  const rowNumber = this.getProjectRowNumber(oldProjectCode);

  // 현재 선택된 사업자/담당자 가져오기
  const headerContainer = document.getElementById(`header-${oldProjectCode}`);
  const basicCard = document.getElementById(`card-basic-${oldProjectCode}`);

  let company = this.currentProject['사업자'];
  let manager = this.currentProject['담당자'];

  // 편집 중인 값 가져오기
  if (basicCard) {
    const companySelect = basicCard.querySelector('[data-field="사업자"]');
    if (companySelect && companySelect.tagName === 'SELECT') {
      company = companySelect.value;
    }
  }

  if (headerContainer) {
    const managerSelect = headerContainer.querySelector('[data-field="담당자"]');
    if (managerSelect && managerSelect.tagName === 'SELECT') {
      manager = managerSelect.value;
    }
  }

  // 새 프로젝트 코드 생성
  const newProjectCode = this.generateProjectCode(rowNumber, company, manager);

  // 헤더 배지 텍스트 업데이트
  const codeBadge = headerContainer.querySelector('.project-code-badge');
  if (codeBadge) {
    const icon = codeBadge.querySelector('i');
    const iconHtml = icon ? icon.outerHTML : '<i class="fas fa-tag me-2"></i>';

    codeBadge.innerHTML = `${iconHtml}${newProjectCode}`;

    // 변경되었음을 시각적으로 표시 (옵션)
    if (newProjectCode !== oldProjectCode) {
      codeBadge.classList.add('code-changed');
      setTimeout(() => codeBadge.classList.remove('code-changed'), 1000);
    }
  }

  // currentProject 업데이트 (저장 시 사용)
  this.currentProject['프로젝트 코드'] = newProjectCode;

  return newProjectCode;
}
```

## 3.4 이벤트 리스너 바인딩

```javascript
/**
 * 헤더 필드 이벤트 바인딩
 */
bindHeaderFieldEvents(projectCode) {
  const headerContainer = document.getElementById(`header-${projectCode}`);
  if (!headerContainer) return;

  // 담당자 변경 시
  const managerSelect = headerContainer.querySelector('[data-field="담당자"]');
  if (managerSelect) {
    managerSelect.addEventListener('change', () => {
      this.updateHeaderProjectCode(projectCode);
    });
  }

  // 기본정보 카드의 사업자 변경 시
  const basicCard = document.getElementById(`card-basic-${projectCode}`);
  if (basicCard) {
    const companySelect = basicCard.querySelector('[data-field="사업자"]');
    if (companySelect) {
      companySelect.addEventListener('change', () => {
        this.updateHeaderProjectCode(projectCode);
      });
    }
  }
}
```

================================================================================
4. 저장 후 DataTable 업데이트
================================================================================

## 4.1 프로젝트 코드 변경 시 처리

```javascript
async _executeSave(projectCode) {
  // ... (기존 로직)

  const result = await response.json();

  if (result.success) {
    const finalProjectCode = result.project_code || projectCode;
    const projectCodeChanged = result.old_project_code &&
                              result.old_project_code !== finalProjectCode;

    if (projectCodeChanged) {
      console.log(`🔄 [프로젝트 코드 변경] ${result.old_project_code} → ${finalProjectCode}`);

      // 1. DataTable 행 ID 업데이트
      await this.updateDataTableRow(result.old_project_code, finalProjectCode, result.project);

      // 2. 현재 프로젝트 코드 업데이트
      this.currentProjectCode = finalProjectCode;

      // 3. 아코디언 유지 (재오픈)
      this.reopenAccordion(finalProjectCode, result.project);

      return;
    }

    // ... (기존 로직)
  }
}
```

## 4.2 DataTable 행 업데이트

```javascript
/**
 * DataTable 행 업데이트 (프로젝트 코드 변경 시)
 */
async updateDataTableRow(oldCode, newCode, projectData) {
  if (!this.dataTable) return;

  const row = this.dataTable.row(`#row-${oldCode}`);
  if (!row || row.index() === undefined) {
    console.warn(`⚠️ 행을 찾을 수 없음: ${oldCode}`);
    return;
  }

  // 1. 행 데이터 업데이트
  row.data(projectData);

  // 2. 행 ID 업데이트 (DOM 조작)
  const rowNode = row.node();
  if (rowNode) {
    rowNode.id = `row-${newCode}`;
    rowNode.setAttribute('data-project-code', newCode);
  }

  // 3. 화면 갱신 (draw 호출하지 않고 node만 업데이트)
  console.log(`✅ DataTable 행 업데이트: ${oldCode} → ${newCode}`);
}

/**
 * 아코디언 재오픈 (프로젝트 코드 변경 후)
 */
reopenAccordion(newProjectCode, projectData) {
  // 1. 기존 아코디언 닫기 (애니메이션 없이)
  this.closeAccordion(false);

  // 2. 새 프로젝트 코드로 아코디언 열기
  setTimeout(() => {
    const newRow = this.dataTable.row(`#row-${newProjectCode}`).node();
    if (newRow) {
      this.openAccordion(newRow, projectData);
    }
  }, 100);
}
```

================================================================================
5. CSS 애니메이션
================================================================================

```css
/* 프로젝트 코드 변경 애니메이션 */
.project-code-badge.code-changed {
  animation: codeChangeFlash 1s ease;
}

@keyframes codeChangeFlash {
  0%, 100% {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  }
  50% {
    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
    transform: scale(1.05);
  }
}
```

================================================================================
6. 구현 순서 (업데이트)
================================================================================

## Phase 1: 담당자 드롭다운 (동적 생성)
1. ✅ `getActiveManagersFromData()` 함수 작성
2. ✅ `generateManagerDropdown()` 함수 작성
3. ✅ 테스트: 퇴사자 제외, 신규 담당자 포함 확인

## Phase 2: 프로젝트 코드 클라이언트 생성
1. ✅ `generateProjectCode()` 함수 작성
2. ✅ `getProjectRowNumber()` 함수 작성
3. ✅ 테스트: 백엔드와 동일한 코드 생성 확인

## Phase 3: 헤더 프로젝트 코드 실시간 업데이트
1. ✅ `updateHeaderProjectCode()` 함수 작성
2. ✅ `bindHeaderFieldEvents()` 함수 작성
3. ✅ 담당자 드롭다운 change 이벤트 연결
4. ✅ 사업자 드롭다운 change 이벤트 연결
5. ✅ 테스트: 드롭다운 변경 시 헤더 텍스트 즉시 변경 확인

## Phase 4: DataTable 행 업데이트
1. ✅ `updateDataTableRow()` 함수 작성
2. ✅ `reopenAccordion()` 함수 작성
3. ✅ 저장 성공 시 DataTable 행 ID 변경
4. ✅ 테스트: 프로젝트 코드 변경 후 행 클릭 확인

## Phase 5: 헤더 필드 편집 (기존 계획)
1. ✅ `generateHeaderFields()` 함수 작성
2. ✅ 현장주소/공사내용 입력창
3. ✅ CSS 스타일 추가
4. ✅ 변경사항 수집 (`collectAllChanges()` 수정)

## Phase 6: 빌드 및 테스트
1. ✅ CSS 파일 작성
2. ✅ npm run build
3. ✅ 전체 시나리오 테스트

================================================================================
7. 테스트 시나리오 (업데이트)
================================================================================

## 시나리오 1: 담당자만 변경 (실시간 업데이트)
1. G2890-SH 아코디언 열기
2. 편집 버튼 클릭
3. 담당자 "강성환" → "박정우" 선택
4. **즉시 헤더 프로젝트 코드 변경: G2890-SH → G2890-JW**
5. 저장 버튼 클릭
6. 백엔드 저장 완료
7. **DataTable 행 ID 업데이트: row-G2890-SH → row-G2890-JW**
8. **DataTable 셀 텍스트 업데이트: G2890-SH → G2890-JW**
9. 아코디언 재오픈 (G2890-JW로)

## 시나리오 2: 사업자 변경 (실시간 업데이트)
1. G2890-SH 아코디언 열기
2. 편집 모드
3. 기본정보 카드 → 사업자 "글로벌" → "플렌트" 선택
4. **즉시 헤더 프로젝트 코드 변경: G2890-SH → P2890-SH**
5. 저장
6. DataTable 업데이트

## 시나리오 3: 사업자 + 담당자 동시 변경
1. 편집 모드
2. 사업자: "글로벌" → "글로벌그룹"
3. **헤더 업데이트: G2890-SH → R2890-SH**
4. 담당자: "강성환" → "심장원"
5. **헤더 업데이트: R2890-SH → R2890-SJW**
6. 저장
7. DataTable 업데이트

## 시나리오 4: 신규 담당자 등장
1. 관리자가 Google Sheets에서 새 프로젝트 추가
2. 담당자: "김철수" (신규)
3. 프로젝트 목록 새로고침
4. 아코디언 편집 모드 진입
5. **담당자 드롭다운에 "김철수" 자동 추가됨**

## 시나리오 5: 취소 버튼
1. 편집 모드
2. 담당자 변경 → 헤더 프로젝트 코드 변경됨
3. 취소 버튼 클릭
4. **헤더 프로젝트 코드 원래 값으로 복원**
5. this.currentProject도 원래 값 유지

================================================================================
결론
================================================================================

✅ **담당자 드롭다운: 퇴사자 제외, 실사용 담당자만 동적 생성**
✅ **프로젝트 코드 실시간 업데이트: 사업자/담당자 변경 즉시 헤더 텍스트 변경**
✅ **DataTable 행 업데이트: 저장 후 프로젝트 코드 변경 시 행 ID + 셀 텍스트 업데이트**
✅ **아코디언 재오픈: 프로젝트 코드 변경 후에도 아코디언 유지**
✅ **취소 기능: 헤더 프로젝트 코드 복원**

**다음: 코드 작성 시작**
